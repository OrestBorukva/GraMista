/**
 * Однокористувацька фаза: усі сервіси фільтрують по userId, але реальний auth — Крок 8.
 * До того весь стан належить одному дефолтному користувачу з цим стабільним id.
 */
export const DEFAULT_USER_ID = 'default';
