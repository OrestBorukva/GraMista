import type { ReactNode } from 'react';

export const metadata = { title: 'Забули пароль' };

export default function ForgotPasswordLayout({ children }: { children: ReactNode }) {
  return children;
}
